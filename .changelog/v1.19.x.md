# Release v1.19.x - Changelog

Released: YYYY-MM-DD

## Overview

GSD workflow improvements with smart state detection.

## Features

- **GSD smart state detection** — the GSD tab now detects which `.planning/` artifacts exist (codebase map, PROJECT.md, ROADMAP.md, STATE.md, CONCERNS.md) and shows a stepped setup guide with the next command to run, replacing the old binary "not found" empty state
